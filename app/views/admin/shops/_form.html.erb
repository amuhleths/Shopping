<%= form_for([:admin, @shop]) do |f| %>
  <% if @shop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shop.errors.count, "error") %> prohibited this shop from being saved:</h2>

      <ul>
      <% @shop.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, t('.lbl_name') %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :address , t('.lbl_address')%><br />
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <%= f.label :telephone, t('.lbl_telephone') %><br />
    <%= f.text_field :telephone %>
  </div>
  <div class="field">
    <%= f.label :logo_url, t('.lbl_logo_url') %><br />
    <%= f.text_field :logo_url %>
  </div>
  <div class="actions">
    <%= f.submit t('.btn_save')%>
  </div>
<% end %>
